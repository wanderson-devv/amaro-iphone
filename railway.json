{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev"
  },
  "deploy": {
    "startCommand": "npx tsx src/server.ts",
    "healthcheckPath": "/v1/auth/me",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
